<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhuhuibao.mybatis.memCenter.mapper.PositionMapper" >
  <resultMap id="BaseResultMap" type="com.zhuhuibao.mybatis.memCenter.entity.Position" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="parentId" property="parentId" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, name, parentId
  </sql>
  <select id="findPosition" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from t_dictionary_position
    where parentId = 0 limit 0,#{size}
  </select>
  <select id="findSubPosition" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from t_dictionary_position
    where parentId != 0
  </select>
  <select id="findById" resultType="java.util.Map">
    select * from t_dictionary_position where id = #{id}
  </select>
</mapper>